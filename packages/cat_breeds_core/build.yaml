targets:
  $default:
    builders:
      freezed:
        generate_for:
          include:
            - "lib/src/helpers/**.dart"
            - "lib/src/failures/**.dart"
      source_gen|combining_builder:
        options:
          ignore_for_file:
            - "type=lint"
